language: node_js

node_js:
  - 7

services:
    - docker

before_install:
    - docker build -t elements-rest .
    - docker run -dp 8080:8080 elements-rest
    - docker ps -a
    - docker run elements-rest /bin/sh -c "npm test"

script:
  - npm test
